# Base URL of the backend API. Leave empty in development — Vite's dev server
# proxies /api/* straight to the backend (see vite.config.js). In production,
# point this at your deployed backend's origin.
# Example: VITE_API_BASE_URL=https://api.lokhainnovation.com
VITE_API_BASE_URL=
